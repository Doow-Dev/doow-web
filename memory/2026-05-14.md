# 2026-05-14 — doow-web

## Done
- Added `.agents/skills/doow-docs-writer/references/connection-source-docs-dna.md` to encode the corrected Doow integrations frame: integrations are connection-source docs for bringing customer data into Doow, not the full docs system or a generic provider catalog. It captures reader anxiety, source-choice framing, ownership, trust, proof, recovery, production readiness, heading spine, and anti-patterns.
- Updated `.agents/skills/doow-docs-writer/SKILL.md` to require reading `connection-source-docs-dna.md` before sweeping integration hubs, chooser pages, source-category pages, or integration pages.
- Updated `.agents/skills/doow-docs-writer/references/doow-docs-patterns.md` to remove the old fixed "recommended first setup" sequence from the hub/chooser pattern and replace it with source-family orientation, outcome-based choice, exact setup routing, and non-mandatory next steps.
- Updated `.agents/skills/doow-docs-writer/references/review-rubric.md` with connection-anxiety checks, ownership checks, no-universal-sequence checks, and trust/data-boundary checks.
- Mirrored the updated Doow docs skill to `/home/ayodele/.agents/skills/doow-docs-writer/`. Repo and home copies diff clean. `quick_validate.py` passes for both copies. ASCII grep passes for both copies.
- Reviewed `.agents/skills/doow-docs-writer/` (SKILL.md + 5 reference files). Graded 8.5/10: strong on structure, scope, product truth, MDX governance, source-pattern attribution. Weak on sentence-level taste.
- After user counter-review, retracted two claims from my grade: (a) the short `agents/openai.yaml` is within spec, not a defect; (b) "doodlestein's skill almost certainly covers this" was speculation that violated the skill's own "borrow patterns not vibes" rule.
- Authored `.agents/skills/doow-docs-writer/references/prose-rules.md` (367 lines). Sections: Doow voice (anchored to Stytch hub + Stripe reference), first-sentence recipe branched 4 ways by page type, paragraph rhythm, subtraction discipline, banned-phrase list with grep command, 6 before/after rewrites, anti-pattern catalog (includes the no-speculation rule), thin-page detector per page type, finishing-pass checklist.
- Updated `SKILL.md` — added a "Finishing pass" section between "Review bar" and "Verification" listing the 5 prose checks + a pointer to the thin-page detector.
- Updated `references/review-rubric.md` — added a "Prose quality" category before MDX/governance with 6 bullets.
- Mirrored the skill to `/home/ayodele/.agents/skills/doow-docs-writer/`. Diff between repo and home: clean (identical).
- Ran `quick_validate.py` against the skill: "Skill is valid!" Ran ASCII grep on both copies: clean.
- Pulled and read external benchmark docs skills via web search + WebFetch: Anthropic `doc-coauthoring` (anthropics/skills, 73k stars), `keithpatton/diataxis-agent-skill`, `sammcj/agentic-coding writing-documentation-with-diataxis`, `github/awesome-copilot/documentation-writer`. Wrote a comparison: our skill wins on domain-tailoring, code-grounded MDX lock, product-truth-first, verification commands, source-pattern attribution. Loses to `doc-coauthoring` only on the absence of a reader-testing step (sub-agent simulation of a cold reader).

## In Progress
- Awaiting user go-ahead to draft `references/reader-testing.md` and wire it into SKILL.md's Finishing pass. That single addition would put our skill above `doc-coauthoring` for Doow's use case.

## Next
- Add `references/reader-testing.md` if approved: a sub-agent invocation pattern that takes the page content + 3-5 reader questions, summarizes what Reader Claude got right/wrong, and loops back to refinement when ambiguous. Borrow verbatim from `doc-coauthoring` Stage 3.
- Decide whether to fold sammcj's per-quadrant language patterns ("you will see" for tutorials, "if X, do Y" for how-tos) into `prose-rules.md`.
- Decide whether to add four explicit operating modes (Classify, Audit, Generate, Restructure) borrowed from `diataxis-agent-skill`. Audit and Restructure are the new verbs that would help with sweeping the 40 existing integration MDX files.
- Skill changes still uncommitted. Also uncommitted: `memory/2026-05-12.md`, `memory/2026-05-14.md`. Pending user direction on whether to bundle the prose-rules work into a focused commit on `animations` branch.

## Notes
- The reader-testing gap is the single highest-leverage borrowable move. Everything else from the competing skills is either nice-to-have or duplicates what we already do.
- The skill-creator `quick_validate.py` lives at `/home/ayodele/.claude/skills/.system/skill-creator/scripts/quick_validate.py` and accepts a skill directory path. Useful in future skill work.
- ASCII discipline matters for these skill files — the existing 5 reference files plus the new `prose-rules.md` and the updates to `SKILL.md` / `review-rubric.md` all pass `LC_ALL=C rg -n "[^\x00-\x7F]"`. Maintain this.
- Pash's tweet (https://x.com/pashmerepat/status/2053617807955578923) crystallized the framing: structural skill keeps a page from being broken; prose skill keeps a page from being stiff/bloated/AI-written. The new file is the second half of that.
