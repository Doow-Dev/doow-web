# 2026-05-04

## Subscriptions manual-tracking renewal video

- Added the supplied `/home/ayodele/Downloads/feature-3.mp4` as a local
  temporary subscriptions visual asset at
  `/assets/illustrations/subscriptions-renewals-feature.mp4`.
- Updated the second subscriptions page section so “Renewals get missed” is the
  default selected item and renders the MP4 instead of the static renewal visual.
- Kept the video muted, looping, inline, autoplaying, and reduced-motion-safe.
- Verified with `npm run typecheck`, `npm run lint`, and a browser check on
  `http://localhost:3001/subscriptions` confirming the video is selected,
  loaded, unpaused, and advancing.

## Blog Sprint 12 launch content and acceptance

- Replaced the launch-facing sample article with
  `/blog/running-out-of-runway` and added a permanent redirect from
  `/blog/sample-post`.
- Added two more real launch posts so the blog now has 3 published posts across
  3 categories: engineering, finance operations, and product.
- Updated Sprint 2, 4, and 11 runtime checks to validate the real launch slug
  and the sample-post redirect.
- Tightened the article header author treatment so the profile chip remains
  beside the smaller bio and social links.
- Removed the detail-page crash-image fallback when a post has no real CDN
  cover, avoiding placeholder media on launch articles.
- Verified with `npm run typecheck`, `npm run lint`, `npm run build`,
  `BASE_URL=http://localhost:3004 npm run check:blog:s6`,
  `BASE_URL=http://localhost:3004 npm run check:blog:s11`,
  `BLOG_LIVE=true npm run build`, and
  `BLOG_LIVE=true BASE_URL=http://localhost:3004 npm run check:blog:launch`.
  Browser smoke covered desktop and mobile article rendering.

## Blog Sprint 11 shell integration and launch prep

- Added launch-prep runtime coverage through `check:blog:s11` and strengthened
  `check:blog:launch` so `BLOG_LIVE=true` is blocked until launch content has
  at least 3 published posts across at least 2 categories.
- Recorded launch media policy: real blog launch covers and author avatars must
  use CDN-relative content paths through the blog media helpers; `/public`
  Figma images are temporary fallbacks only.
- Aligned runtime checks with the current Sprint 6 redirect architecture and
  restored over-range pagination 404s for `/blog/page/999` style routes.
- Added query-aware blog metadata and visible/JSON-LD breadcrumbs for category,
  tag, and paginated `/blog` archive views.
- Routed real post cover images and author avatars through named blog CDN
  helpers while keeping Figma assets as fallbacks, added reduced-motion
  suppression, removed hardcoded muted grays, and tightened card accessibility.
- Verified with `npm run typecheck`, `npm run lint`, `npm run build`,
  `BASE_URL=http://localhost:3001 npm run check:blog:s6`,
  `BASE_URL=http://localhost:3001 npm run check:blog:s11`, and
  `BLOG_LIVE=true npm run build`. The final launch runtime check correctly
  fails on the content gate until launch content is added.

## Blog Sprint 10 author profiles

- Preserved the existing article-header author profile chip and removed the
  mistaken duplicate below-article author block.
- Extended author data with optional social links and wired them into the
  existing header author chip when content provides them.
- Confirmed existing author JSON-LD still renders on `/blog/sample-post`.
- Re-verified with `npm run typecheck`, `npm run lint`, and browser/HTML
  smoke checks on `/blog/sample-post`; full build follows this correction.

## Blog Sprint 6 pagination wiring

- Added real `POSTS_PER_PAGE` pagination helpers for all posts, categories,
  and tags.
- Replaced the static Figma pagination mock with a reusable `BlogPagination`
  component that preserves the existing `.blog-pagination` classes and hides
  when only one page exists.
- Added paginated index, category, and tag routes with page-1 redirects,
  over-range 404s, and sitemap entries for valid page 2 and later URLs.
- Updated the Sprint 6 runtime check to use `/blog/tag/runway/page/1`.
- Verified with `npm run typecheck`, `npm run lint`, `npm run build`,
  `BASE_URL=http://localhost:3001 npm run check:blog:s6`, browser smoke checks,
  and a temporary high-volume post test that was removed before final checks.

## Blog Sprint 7 enhanced article features

- Extracted the article detail table of contents into a client component using
  `post.toc`, active heading state, working hash links, and mobile collapse
  behavior while preserving the desktop rail styling.
- Extracted related posts into a reusable component backed by real related or
  same-category post data, capped at three posts, with no repeated current-post
  filler.
- Added article reading time to the existing header date line and kept compact
  related cards on the existing Figma card treatment.
- Verified with `npm run typecheck`, `npm run lint`, `npm run build`, and
  browser smoke checks on `/blog/sample-post` for TOC links, active state,
  mobile collapse, and no horizontal overflow.

## Blog category strip routing

- Replaced the main blog page's static category pill spans with real links to
  the Sprint 5 category archive routes.
- Removed non-taxonomy placeholder pills from the main blog strip and kept only
  `All`, `Finance operations`, `Product`, `Engineering`, and `Company`.
- Verified with `npm run typecheck`, `npm run lint`, `npm run build`,
  `BASE_URL=http://localhost:3001 npm run check:blog:s5`, and browser DOM checks.

## Blog Sprint 5 archive polish

- Corrected category and tag archive pages to use the same image-backed compact
  blog cards as the main `/blog` grid.
- Restyled archive pages with a polished hero, library band, card grid, and tag
  panel so `/blog/tag/runway` and category archives no longer look like fallback
  pages.
- Verified with `npm run typecheck`, `npm run lint`, `npm run build`, browser
  screenshots, and `BASE_URL=http://localhost:3001 npm run check:blog:s5`.

## Blog Sprint 5 taxonomy archives

- Added category and tag archive routes for `/blog/category/[category]` and
  `/blog/tag/[tag]`.
- Added normalized tag helpers, category/tag filtering, archive counts,
  metadata, breadcrumb JSON-LD, archive analytics events, tag cloud UI, and
  category/tag sitemap entries.
- Aligned the S5 runtime check with the current sample content:
  `/blog/category/engineering` and `/blog/tag/runway`.
- Verified with `npm run typecheck`, `npm run lint`, `npm run build`,
  `BASE_URL=http://localhost:3001 npm run check:blog:s5`, and browser smoke
  checks for `/blog/category/engineering` and `/blog/tag/runway`.

## Blog Sprint 4 distribution

- Added the primary blog RSS feed builder and `/blog/rss.xml` route.
- Wired RSS autodiscovery through the blog index metadata helper.
- Confirmed the base sitemap still includes `/blog` and `/blog/sample-post`;
  parallel Sprint 5 work has also added category and tag sitemap entries.
- Verified the combined tree with `npm run typecheck`, `npm run lint`,
  `npm run build`, and `BASE_URL=http://localhost:3001 npm run check:blog:s4`.

## Blog Sprint 3 acceptance pass

- Re-ran final gates for the blog UI work: `npm run typecheck`,
  `npm run lint`, and `npm run build` all passed.
- Smoke-tested `/blog` and `/blog/sample-post` on the active Doow web server at
  `http://localhost:3001`.
- Verified header Blog navigation, mobile menu Blog navigation, article TOC hash
  targets, related-section `/blog` link target, and no horizontal overflow at
  desktop, tablet, and mobile widths.

## Blog detail TOC and related navigation fix

- Fixed the blog detail table of contents by backing it with real MDX heading
  anchors instead of render-time paragraph IDs.
- Scoped the Figma detail article headings so they provide stable hash targets
  without changing the current visual layout.
- Pointed the related-section **View all** link at `/blog`, which exists in the
  current route set.
- Verified with `npm run typecheck`, `npm run lint`, `npm run build`, and an
  isolated browser check on `http://localhost:3001/blog/sample-post`.
