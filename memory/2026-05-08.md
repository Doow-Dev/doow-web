# 2026-05-08 — doow-web

## 2026-05-08T15:35Z | [doow-web] | [Codex]

## Done

- [doow-web] Implemented the first premium docs UI pass using the agreed
  Clerk + Firecrawl direction. Added a product-grade docs shell, active
  navigation, search modal, polished homepage, article right rail, upgraded MDX
  blocks, code copy controls, and responsive layout improvements.
- [doow-web] Verified the docs UI with desktop and mobile screenshots, tested
  search interaction, ran `pnpm --filter @doow/docs build`, and ran full
  `pnpm verify`.

## Next

- [doow-web] Review the live docs pages with the user, then decide whether to
  commit the docs UI polish as a follow-up commit after visual approval.

## 2026-05-08T14:59Z | [doow-web] | [Codex]

## Done

- [doow-web] Created `/tmp/doow-web-clean-check` from the current migration
  tree while excluding `.git`, `node_modules`, `.next`, Turbo caches, and local
  env files. Ran `pnpm install --frozen-lockfile` and full `pnpm verify` from
  that clean copy with an empty Turbo cache; both passed.
- [doow-web] Updated the final acceptance checklist to mark the frozen-install
  clean-copy gate complete, with a note that CI must repeat the check after the
  migration is committed.

## Next

- [doow-web] Commit the migration and validate Vercel previews/domains for the
  independent web and docs deployments.

## 2026-05-08T14:52Z | [doow-web] | [Codex]

## Done

- [doow-web] Ran the final repo-owned monorepo acceptance pass. Added
  `apps/docs/src/app/sitemap.ts` so the docs app now ships `/sitemap.xml` from
  the docs content loader and `NEXT_PUBLIC_DOCS_SITE_URL`.
- [doow-web] Verified local frozen install, web build, docs build, docs content
  validation, runtime smoke for web/blog/docs, docs search, docs redirect,
  docs sitemap, docs canonical metadata, a negative boundary test, and full
  `pnpm verify`.
- [doow-web] Updated `docs/monorepo-implementation-sprints.md` to mark all
  repo-owned final acceptance items complete while leaving clean-clone and
  Vercel/DNS deployment validation open until the migration is committed and
  external project access is available.

## Next

- [doow-web] Commit the monorepo migration, run `pnpm install --frozen-lockfile`
  from a fresh clone or CI checkout, then validate Vercel previews and domains
  for `www.doow.co` and `docs.doow.co`.

## 2026-05-08T14:30Z | [doow-web] | [Codex]

## Done

- [doow-web] Completed Sprint 8 documentation and agent operating rules. Added
  decision records under `docs/decisions`, monorepo architecture docs under
  `docs/architecture`, and content runbooks under `docs/runbooks`.
- [doow-web] Updated `README.md`, `AGENTS.md`, `CLAUDE.md`, and `SKILL.md` with
  the production monorepo read order, workspace ownership, root commands, app
  boundaries, shared package boundaries, content runbooks, and deployment paths.
  Corrected the web/docs deployment runbooks to show the actual Vercel
  `cd ../.. && pnpm ...` commands.
- [doow-web] Marked Sprint 8 complete in
  `docs/monorepo-implementation-sprints.md` and verified with
  `pnpm check:boundaries`, `pnpm check:deployment`, `git diff --check`, a
  guidance grep, and full `pnpm verify`.

## Next

- [doow-web] Work through the final acceptance checklist and close the remaining
  external Sprint 7 items in Vercel and DNS when credentials are available:
  deploy previews, production domains, and independent web/docs production
  routing.

## 2026-05-08T13:33Z | [doow-web] | [Codex]

## Done

- [doow-web] Completed the repo-owned Sprint 7 deployment setup: added
  `apps/web/vercel.json`, `apps/docs/vercel.json`, docs app env examples, web
  canonical env examples, `tools/check-deployment-config.mjs`, root
  `pnpm check:deployment`, and CI deployment-config validation.
- [doow-web] Added deployment runbooks for web, docs, rollback,
  environment-variable changes, and Vercel project setup under
  `docs/deployment`. Verified app-owned env separation and canonical URL source
  ownership with `pnpm check:deployment`, then ran app builds and full
  `pnpm verify`.

## Next

- [doow-web] Complete the external Sprint 7 actions in Vercel and DNS:
  create or update the `@doow/web` and `@doow/docs` projects, set root
  directories to `apps/web` and `apps/docs`, assign `www.doow.co` and
  `docs.doow.co`, verify deploy previews, and confirm both domains route to the
  correct apps.

## 2026-05-08T13:26Z | [doow-web] | [Codex]

## Done

- [doow-web] Completed Sprint 6 boundary enforcement and CI: added
  `tools/check-boundaries.mjs`, root `pnpm check:boundaries`, and made root
  `pnpm verify` run boundaries before lint, typecheck, tests, content checks,
  and builds. The checker blocks web/docs cross-imports, package-to-app
  imports, cross-workspace relative imports, circular workspace dependencies,
  undeclared dependencies, and missing internal package declarations.
- [doow-web] Added `.github/workflows/ci.yml` with frozen pnpm install,
  boundary checks, lint, typecheck, tests, content checks, separate web/docs
  builds, and root verification. Fixed real boundary findings by declaring
  `server-only` in `@doow/web` and `eslint-config-next` as a peer of
  `@doow/config`.
- [doow-web] Verified failure gates with temporary forbidden app import and
  temporary invalid docs link, then restored valid content. Ran CI-equivalent
  local commands and full `pnpm verify`.

## Next

- [doow-web] Start Sprint 7 by preparing deployment settings and runbooks for
  separate web and docs production apps, including app root directories, build
  commands, domains, environment variable ownership, and rollback steps.

## 2026-05-08T13:20Z | [doow-web] | [Codex]

## Done

- [doow-web] Completed Sprint 5 docs content governance: added graph-level
  docs validation for duplicate slugs, duplicate heading IDs, internal links,
  redirects, duplicate redirect sources, search records, navigation coverage,
  and launch versioning policy. Added `apps/docs/src/lib/docs/redirects.ts`,
  `search.ts`, `versioning.ts`, `governance.ts`, and static
  `/search.json`.
- [doow-web] Added docs content maintenance tooling for redirect tracing,
  move-with-redirect, and delete-with-inbound-link checks. Verified negative
  gates by temporarily forcing unsupported MDX and a broken internal link to
  fail `pnpm --filter @doow/docs check:content`, then restored valid content.
  Runtime-smoked `/search.json` and `/start` redirect on port `3103`, and ran
  full root `pnpm verify`.

## Next

- [doow-web] Start Sprint 6 by adding import boundary enforcement and CI checks
  so apps cannot import each other, packages cannot import apps, and fresh
  installs can run the full verification loop.

## 2026-05-08T13:14Z | [doow-web] | [Codex]

## Done

- [doow-web] Completed Sprint 4 docs app foundation: added `apps/docs` as
  `@doow/docs`, with Next.js app routes for `/`, `/getting-started`, `/guides`,
  `/reference`, and `/changelog`; app-owned docs content under
  `apps/docs/content/docs`; docs schema, navigation, slug/path resolver, MDX
  config, content loader, and content gate under `apps/docs/src/lib/docs`; and
  docs shell, article layout, and approved MDX components under
  `apps/docs/src/components/docs`.
- [doow-web] Verified `@doow/docs` dev/build/content gates, runtime-smoked all
  docs routes on port `3102`, confirmed the sample MDX page renders a table of
  contents and approved components, and ran full root `pnpm verify` with both
  `@doow/web` and `@doow/docs` in the Turbo graph.

## Next

- [doow-web] Start Sprint 5 by adding Clerk-style docs content governance:
  duplicate slug checks, duplicate heading ID checks, stricter image/link
  validation, redirect registry validation, unsafe HTML checks, and unsupported
  MDX component reporting.

## 2026-05-08T12:39Z | [doow-web] | [Codex]

## Done

- [doow-web] Completed Sprint 3 shared package foundation: added
  `@doow/config`, `@doow/design-tokens`, `@doow/content-schemas`, and
  `@doow/mdx`; moved generic blog MDX parsing, heading extraction, component
  validation, image validation, link validation, and code-block extraction into
  `@doow/mdx`; reused shared frontmatter primitives from
  `@doow/content-schemas` while keeping the blog schema app-owned.
- [doow-web] Added Vitest coverage for `@doow/content-schemas` and
  `@doow/mdx`, wired `apps/web` to transpile shared packages, approved
  `esbuild` for pnpm build scripts, and verified blog routes with a runtime
  smoke test.

## Next

- [doow-web] Start Sprint 4 by creating `apps/docs` as `@doow/docs` with a
  production docs app skeleton, app-owned docs schema, MDX component map, and
  sample routes/content that consume the shared packages.

## 2026-05-08T12:07Z | [doow-web] | [Codex]

## Done

- [doow-web] Completed Sprint 2 app relocation: moved the current Next.js app
  into `apps/web`, moved blog MDX/content assets with it, created
  `@doow/web`, updated root scripts to delegate through pnpm/Turbo filters,
  adjusted Next/Turbopack and Playwright config for the workspace layout, and
  kept ignored build artifacts out of status.
- [doow-web] Verified Sprint 2 with `pnpm --filter @doow/web lint`,
  `typecheck`, `test:unit`, `check:content`, `build`, a runtime smoke test for
  landing/blog/rss/search routes, root `pnpm lint`, `pnpm typecheck`,
  `pnpm test`, `pnpm check:content`, `pnpm build`, and root `pnpm verify`.

## Next

- [doow-web] Start Sprint 3 by adding the production docs app skeleton in
  `apps/docs` and wiring it to the monorepo without touching current web
  runtime behavior.

## 2026-05-08T12:05Z | [doow-web] | [Codex]

## Done

- [doow-web] Completed Sprint 1 root monorepo foundation: added
  `pnpm-workspace.yaml`, `turbo.json`, `.npmrc`, pnpm metadata, transitional
  root scripts, `turbo`, `pnpm-lock.yaml`, pnpm build-script approvals, and
  `turbopack.root`. Removed `package-lock.json`, pinned package versions to the
  npm-lock baseline, and marked Sprint 1 checklist items complete.

## Next

- [doow-web] Start Sprint 2 by moving the current app into `apps/web` while
  preserving blog content loading and current route behavior.

## 2026-05-08T11:39Z | [doow-web] | [Codex]

## Done

- [doow-web] Added `docs/monorepo-implementation-sprints.md` as the working
  checklist for the production monorepo and docs-system migration. It breaks the
  work into Sprint 0 through Sprint 8, with task checkboxes, gates, and final
  acceptance checks so progress can be marked as implementation lands.

## Next

- [doow-web] Start Sprint 0 by creating the migration branch or worktree and
  recording the current baseline checks before changing repo structure.

## 2026-05-08T11:23Z | [doow-web] | [Codex]

## Done

- [doow-web] Corrected `docs/monorepo-migration-guide.md` so the target is
  Clerk-style Doow-owned MDX content governance, not Fumadocs or Nextra by
  default. Added the missing production items: package build strategy,
  `transpilePackages`, pnpm lockfile migration, Turbo env/cache rules, docs
  search and versioning decisions, boundary enforcement, content validation
  commands, Vercel deployment settings, and blog content path migration risks.

## Next

- [doow-web] Turn the migration guide into architecture decision records before
  moving files: monorepo, package manager, docs content system, search,
  versioning, and package boundaries.

## 2026-05-08T11:09Z | [doow-web] | [Codex]

## Done

- [doow-web] Updated `docs/monorepo-migration-guide.md` to account for the
  current blog MDX pipeline and Clerk-style docs-content rigor. The guide now
  recommends a shared `@doow/mdx` and `@doow/content-schemas` layer while
  keeping blog and docs schemas, routes, component maps, and navigation
  app-owned.

## Next

- [doow-web] Decide whether docs use Fumadocs' content layer, a Doow-owned MDX
  pipeline, or a hybrid before creating `apps/docs`.

## 2026-05-08T00:00Z | [doow-web] | [Codex]

## Done

- [doow-web] Added `docs/monorepo-migration-guide.md` as the production
  planning guide for moving Doow to a monorepo with `apps/web`, `apps/docs`,
  shared `@doow/*` packages, Turborepo, pnpm workspaces, deployment boundaries,
  agent readup, migration phases, and acceptance checks.

## Next

- [doow-web] Decide whether the docs app uses Fumadocs or Nextra.
- [doow-web] Record the final monorepo decision in
  `docs/decisions/0001-use-monorepo.md` before moving code.
