# 2026-05-10 — doow-web

## 2026-05-10T16:54Z | [doow-web] | [Codex]

## Done

- [doow-web] Moved docs desktop scrolling back to the normal document/browser
  scrollbar at the far-right edge while keeping the sidebar and header pinned
  with fixed positioning.

## Verified

- [doow-web] Passed `pnpm --filter @doow/docs typecheck`.
- [doow-web] Passed `pnpm --filter @doow/docs lint`.
- [doow-web] Browser-checked `http://localhost:3001` with `peruz`; document
  scroll moved, pane/body content stayed non-scrollable, and the sidebar/header
  stayed at the same viewport coordinates.

## 2026-05-10T16:40Z | [doow-web] | [Codex]

## Done

- [doow-web] Moved desktop docs scrolling from `.docs-shell__body` to a new
  `.docs-main-pane` wrapper so the scrollbar sits on the outside edge of the
  right content pane while the content body itself remains a normal flow area.

## Verified

- [doow-web] Passed `pnpm --filter @doow/docs typecheck`.
- [doow-web] Passed `pnpm --filter @doow/docs lint`.
- [doow-web] Browser-checked `http://localhost:3001` with `peruz`; body scroll
  stayed at `0`, `.docs-main-pane` scrolled, `.docs-shell__body` stayed
  `overflow: visible`, and the sidebar/header remained stationary.

## 2026-05-10T16:31Z | [doow-web] | [Codex]

## Done

- [doow-web] Changed the desktop docs shell to Clerk-style frame scrolling:
  the browser body stays locked, the sidebar remains stationary, the header
  stays fixed in the content pane, and `.docs-shell__body` owns vertical page
  scroll.

## Verified

- [doow-web] Passed `pnpm --filter @doow/docs typecheck`.
- [doow-web] Passed `pnpm --filter @doow/docs lint`.
- [doow-web] Browser-checked `http://localhost:3001` with `peruz`; body scroll
  stayed at `0` while `.docs-shell__body` scrolled.

## 2026-05-10T15:44Z | [doow-web] | [Codex]

## Done

- [doow-web] Converted the docs sidebar section labels into Radix accordion
  triggers with chevrons so each menu group can collapse and expand.
- [doow-web] Added a shared `Accordion` primitive in `@doow/ui` and isolated
  docs accordion styling in `apps/docs/src/styles/sidebar-accordion.css`.

## Verified

- [doow-web] Passed `pnpm --filter @doow/ui typecheck`.
- [doow-web] Passed `pnpm --filter @doow/docs typecheck`.
- [doow-web] Passed `pnpm --filter @doow/docs lint`.

## 2026-05-10T15:23Z | [doow-web] | [Codex]

## Done

- [doow-web] Replaced the docs sidebar's native panel scrolling with a reusable
  Radix/shadcn-style `ScrollArea` exported from `@doow/ui`.
- [doow-web] Kept the docs logo outside the scroll viewport and moved the
  scroll thumb into the sidebar content rail, matching the reference docs
  layout.

## Verified

- [doow-web] Passed `pnpm --filter @doow/ui typecheck`.
- [doow-web] Passed `pnpm --filter @doow/docs typecheck`.
- [doow-web] Passed `pnpm --filter @doow/docs lint`.
- [doow-web] Browser-checked the sidebar ScrollArea at `http://localhost:3001`
  with `peruz`; the thumb renders inside the sidebar content viewport.

## 2026-05-10T15:13Z | [doow-web] | [Codex]

## Done

- [doow-web] Matched the docs header and sidebar chrome to the web app's Inter
  typography direction with a stronger docs-specific scale: 14px header/search
  controls, 13px sidebar rows, calmer section labels, and larger control hit
  areas.
- [doow-web] Verified the rendered docs homepage at `http://localhost:3001`
  after the typography update with `peruz` screenshot checks.

## Verified

- [doow-web] Passed `pnpm --filter @doow/docs typecheck`.
- [doow-web] Passed `pnpm --filter @doow/docs lint`.

## 2026-05-09T23:17Z | [doow-web] | [Codex]

## Done

- [doow-web] Reworked the docs desktop chrome so the sidebar column owns the
  left rail from the top, while the search, top nav, and actions sit in the
  right content header like Clerk docs.
- [doow-web] Moved the desktop docs brand into the sidebar rail and kept a
  separate mobile-only brand in the stacked header, removing the full-width
  header structure and duplicate desktop logo.
- [doow-web] Rebalanced the docs header typography after comparing against the
  production web app navbar. Header links, search text, docs label, and CTA
  labels now use the Doow `type-body-sm` scale and `500` weight instead of the
  too-heavy docs-only values.
- [doow-web] Replaced the docs header placeholder "D" mark with the real Doow
  logo assets in `apps/docs/src/components/docs/docs-shell.tsx`.
- [doow-web] Added the light and dark Doow wordmarks under
  `apps/docs/public/logos/` for the docs app public asset boundary.
- [doow-web] Tightened the docs header, search trigger, top nav, and right-side
  actions in `apps/docs/src/styles/globals.css` using a Clerk/TanStack-style
  compact docs header rhythm.

## Verified

- [doow-web] Passed `pnpm --filter @doow/docs typecheck`.
- [doow-web] Passed `pnpm --filter @doow/docs lint`.
- [doow-web] Browser-checked the docs homepage at `http://localhost:3001` in
  desktop light mode, desktop dark mode, and a 390px mobile viewport.
