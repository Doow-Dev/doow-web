# 2026-05-03

## Blog Figma index implementation

- Follow-up: replaced the custom blog-owned Doow AI banner with the reusable
  `SiteFooterPromoSection` and `siteFooterPromoPresets.doowAiQuestions`, then
  removed the duplicate CTA styles, tokens, and review asset.
- Replaced the provisional `/blog` index with the provided Figma nodes:
  `1601:2420`, `1605:3143`, `1605:3168`, and `1601:2647`.
- Reused the global navbar through layout, reused `Badge` and `Button`, and
  extended the existing blog `ArticleCard` instead of creating a separate card
  implementation.
- Added local Figma review assets under `public/blog/figma/` and centralized
  those asset references in `src/lib/blog/figma-assets.ts`.
- Updated `docs/rebuild/blog-s0b-extraction.md` so `/blog` is no longer marked
  blocked on missing Figma nodes; the article page remains transitional.
- Verified `npm run typecheck`, `npm run lint`,
  `NEXT_PRIVATE_BUILD_WORKER_COUNT=1 NODE_OPTIONS=--max-old-space-size=4096 npm run build`,
  `BASE_URL=http://localhost:3004 npm run check:blog:s2`, `200` responses for
  `/blog` and `/blog/sample-post`, and peruz overflow checks at `360`, `390`,
  `768`, `1024`, `1280`, and `1440`.

## Blog governance catch-up

- Closed the blog gate debt before Sprint 3 by adding the launch success definition, category sign-off, authoring guide, launch ops runbook, staged future check scripts, and blog performance targets.
- Registered `check:blog:s4`, `check:blog:s5`, `check:blog:s6`, and `check:blog:launch` while keeping only `check:blog:s2` active for the current implemented routes.
- Verified shell syntax, `npm run lint`, `npm run typecheck`, `npm run build`, and `env BASE_URL=http://localhost:3004 npm run check:blog:s2`.

## Sprint 2 blog routes and SEO safety

- Completed the next blog sprint by adding article/index SEO helpers, BlogPosting/BreadcrumbList/Person JSON-LD, `/api/og/blog`, `/sitemap.xml`, and the `BLOG_LIVE=false` env template note.
- Added `scripts/check-blog/_lib.sh` and `scripts/check-blog/s2.sh`, then registered `npm run check:blog:s2`.
- Verified `npm run lint`, `npm run typecheck`, `npm run build`, `BASE_URL=http://localhost:3003 npm run check:blog:s2`, and visually sanity-checked the generated OG PNG.

## Sprint 1 blog content pipeline

- Implemented the typed `/blog` content pipeline with strict post and author schemas, controlled taxonomy, author resolution, CDN-relative blog asset helpers, AST-based TOC extraction, reading-time computation, and MDX safety validation.
- Rewired the proof `/blog` and `/blog/[slug]` routes to the new pipeline while keeping the existing route styling and MDX render path intact.
- Added `content/blog/authors/doow-team.mdx` and replaced `content/blog/sample-post.mdx` with the Sprint 1 frontmatter model and broad MDX fixture coverage.
- Verified `npm run typecheck`, `npm run build`, local `200` responses for `/blog` and `/blog/sample-post`, and temporary invalid-content failures with named `[blog] <filename>: <field>` errors.

## Blog architecture planning

- Integrated Clerk public docs repo learnings into the existing blog docs without creating a separate Clerk sprint.
- Updated `docs/rebuild/blog-sprints.md`, `docs/rebuild/blog-roadmap.md`, and `docs/rebuild/blog-architecture-reference-audit.md` to require AST-based MDX validation, approved MDX component contracts, richer code block metadata, image governance, duplicate heading ID checks, and launch audit coverage.
- Kept the plan scoped to `/blog` inside the existing Doow landing app, not a new standalone Next.js project.
