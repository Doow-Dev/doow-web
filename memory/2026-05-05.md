# 2026-05-05 — doow-web

## Done
- Blog single-page filter shipped: `/blog` reads `?category=`, `?tag=`, `?page=` and filters in place; legacy `/blog/category/[c]`, `/blog/tag/[t]`, `/blog/page/[n]` (and the `/page/[n]` paginated combos) are now slim 308 redirects to the search-param URL. Latest Article only renders on unfiltered all-posts page 1.
- Blog hero copy locked: H1 stays `Blog` (do not change), subheading is `Reporting and analysis for modern finance teams.`
- `src/components/blog/pagination.tsx` extended with optional `buildHref(page)` so callers can build query-string pagination URLs.
- Walked the user through how the integrations hero "background" is built: base `--bg-subtle` + masked SVG illustration (`integrations-hero-art.svg` clipped by `integrations-hero-mask.svg`) + `surface-gradient-integrations-bottom` overlay (`--gradient-integrations-section-bottom`). No code change.

## In Progress
-

## Next
- Sweep `AGENTS.md` "Current Route Policy" — the redirect-only blog routes are still listed as live routes.
- Check `src/app/sitemap.ts` for any deprecated `/blog/category/*` or `/blog/tag/*` URLs to remove.
- Decide whether `BlogCategoryAnalytics` / `BlogTagAnalytics` should fire from filtered `/blog` (currently only `BlogIndexAnalytics` fires regardless of filter, since the analytics components lived on the old per-category/tag pages that now redirect).
- JSON-LD breadcrumbs for category/tag views are no longer emitted (redirects don't render) — confirm with user whether this matters before adding back.

## Notes
- User's hard rules from this session: don't touch the blog H1, no "playbooks/lessons/field notes" vocabulary, single-page filter only (no separate routes — layout shift was the complaint), plain-English explanations not technical jargon when asked "what do you mean".
- Doow voice cues that worked: imperative-verb landing H1s, triplet subheadings, "in one place" house phrase, "finance teams" audience tag — but the blog hero stays restrained ("Reporting and analysis for modern finance teams") rather than landing-hero-style.
