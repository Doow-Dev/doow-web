# 2026-05-15 - doow-web

## Done

- Completed the final production-readiness pass for the current integrations
  docs sweep. Ran the global banned-phrase/UI-arrow scan across
  `apps/docs/content/docs/integrations`, cleaned remaining UI path shorthand
  such as `Company Settings -> Integrations`, removed the final `should`
  wording hit, and re-ran the scan until it returned no matches.
- Ran `pnpm --filter @doow/docs check:content`; it passed with 43 docs pages
  and 43 search records.
- Ran `pnpm --filter @doow/docs build`; it passed, generating 92 static pages,
  `/search.json`, `/sitemap.xml`, `/llms.txt`, and raw docs routes.
- Ran `git diff --check` across the integration docs, docs navigation, docs
  skill files, and memory log; it returned no whitespace or patch hygiene
  issues.
- Searched for unresolved markers such as TODO, FIXME, VERIFY, needs
  verification, TBD, dummy, placeholder, lorem, and coming soon in the
  integration docs and docs skill references; no matches remained.
- Rewrote `apps/docs/content/docs/integrations/accounting/index.mdx` as the
  Accounting setup guide under the connection-source standard. The page now
  explains when accounting is the right financial-data source versus usage
  data, lists supported providers, defines the secure linking flow and category
  selection, clarifies expense fields Doow reads and sensitive finance/payroll/
  bank data it does not read, explains spend verification, contract
  reconciliation, and cost attribution, and adds recovery paths for missing
  expenses, incomplete vendor totals, unrelated expenses, and currency/entity
  mismatches.
- Ran the prose scan; it returned no banned filler or UI-arrow matches after
  replacing the remaining `should` in the setup steps.
- Ran `pnpm --filter @doow/docs check:content`; it passed with 43 docs pages
  and 43 search records.
- Rewrote `apps/docs/content/docs/integrations/hris/zoho-people.mdx` as the
  Zoho People setup guide under the connection-source standard. The page now
  separates Zoho People HRIS from Zoho identity, names the Zoho People admin
  approval requirement, adds an employee-form read access callout, defines the
  `ZOHOPEOPLE.forms.READ` scope, clarifies employee fields Doow reads and
  sensitive leave, attendance, payroll, appraisal, tax, and other Zoho product
  data it does not read, and adds Zoho People-specific recovery for wrong
  account, hidden/missing fields, and expired consent.
- Checked official Zoho People docs while editing provider-specific language:
  Zoho People APIs extract employee and form data, API access requires an auth
  token or OAuth, scopes use the `service.scope.operation` format, and
  `ZOHOPEOPLE.forms.READ` reads form data including employee records.
- Ran the prose scan; it returned no banned filler or UI-arrow matches after
  patching.
- Ran `pnpm --filter @doow/docs check:content`; it passed with 43 docs pages
  and 43 search records.
- Rewrote `apps/docs/content/docs/integrations/hris/deel.mdx` as the Deel
  setup guide under the connection-source standard. The page now opens with
  employee, contractor, and worker status, names the Deel admin approval
  requirement, adds a payment/legal-document boundary callout, defines worker
  fields Doow reads and sensitive payment, tax, banking, identity document, and
  contract fields it does not read, and adds Deel-specific recovery for denied
  authorization, missing workers, and missing worker status.
- Checked official Deel docs while editing provider-specific language: Deel's
  people endpoint exposes worker profile fields such as work email, job title,
  hiring type/status, contract status, start date, team filters, and
  termination last day; Deel also has separate payroll, payments, contracts,
  and compliance surfaces. Kept the page focused on worker reporting fields.
- Ran the prose scan; it returned no banned filler or UI-arrow matches after
  patching.
- Ran `pnpm --filter @doow/docs check:content`; it passed with 43 docs pages
  and 43 search records.
- Rewrote `apps/docs/content/docs/integrations/hris/gusto.mdx` as the Gusto
  setup guide under the connection-source standard. The page now opens with
  employee records and employment status, names the Gusto company/admin
  approval requirement, adds a payroll-boundary callout, defines HR fields Doow
  reads and sensitive payroll/tax/payment data it does not read, and adds
  Gusto-specific recovery for denied authorization, missing employees, and
  missing employment status.
- Checked official Gusto docs while editing provider-specific language: Gusto
  employee APIs and onboarding/termination flows expose employee and employment
  status concepts, while payroll, termination payroll, tax, contractor payment,
  and bank/payment details are separate sensitive payroll surfaces. Kept the
  page focused on HRIS employee reporting fields only.
- Ran the prose scan; it returned no banned filler or UI-arrow matches after
  patching.
- Ran `pnpm --filter @doow/docs check:content`; it passed with 43 docs pages
  and 43 search records.
- Rewrote `apps/docs/content/docs/integrations/hris/bamboohr.mdx` as the
  BambooHR setup guide under the connection-source standard. The page now
  opens with employee records, departments, job titles, and employment status,
  names the BambooHR subdomain and HR-approved connection requirements, adds a
  permissioned-access callout, defines HR fields Doow reads and sensitive HR
  fields it does not read, and adds BambooHR-specific recovery for wrong
  subdomain, limited employee visibility, and blank HR fields.
- Checked official BambooHR docs while editing provider-specific language:
  BambooHR API requests are made against the company domain, API access follows
  the permissions of the user or API key, employee fields include work email,
  department, job title, supervisor, employment status, hire date, and
  termination date, and field-level permissions can omit fields from responses.
- Ran the prose scan; it returned no banned filler or UI-arrow matches after
  patching.
- Ran `pnpm --filter @doow/docs check:content`; it passed with 43 docs pages
  and 43 search records.
- Rewrote `apps/docs/content/docs/integrations/hris/index.mdx` as the HRIS
  overview and chooser under the connection-source standard. The page now
  explains when HRIS is the right source versus identity, lists BambooHR,
  Gusto, Deel, and Zoho People as setup paths, defines employee fields Doow
  reads and sensitive HR/payroll fields it does not read, explains seat
  attribution, department spend, and headcount accuracy, and routes users to
  exact provider setup pages.
- Ran the prose scan; it returned no banned filler or UI-arrow matches after
  removing the remaining em dash in the field table.
- Ran `pnpm --filter @doow/docs check:content`; it passed with 43 docs pages
  and 43 search records.
- Rewrote `apps/docs/content/docs/integrations/identity/zoho.mdx` as the Zoho
  identity setup guide under the connection-source standard. The page now
  clearly separates Zoho identity from Zoho People HRIS, keeps the documented
  `ZohoDirectory.Users.READ` scope narrow, removes app assignment/group claims
  that the page does not support, tightens read and non-read boundaries, and
  adds Zoho-specific recovery paths for wrong organization, missing HRIS data,
  and revoked or expired OAuth consent.
- Checked official Zoho docs while editing provider-specific language: Zoho
  OAuth scopes define service, module, and operation type; read scopes are
  product-specific; users can revoke app access from Zoho Accounts under
  Sessions and Connected Apps.
- Ran the prose scan; it returned no banned filler or UI-arrow matches after
  patching.
- Ran `pnpm --filter @doow/docs check:content`; it passed with 43 docs pages
  and 43 search records.
- Rewrote `apps/docs/content/docs/integrations/identity/onelogin.mdx` as the
  OneLogin setup guide under the connection-source standard. The page now opens
  with users, groups, app assignments, and sign-in activity from a OneLogin
  account, names the API credential setup owner requirement, makes the
  **Read All** trust moment explicit, tightens read and non-read boundaries,
  and adds OneLogin-specific proof and recovery paths for missing users, app
  assignments, and sign-in activity.
- Checked official OneLogin docs while editing provider-specific language:
  OneLogin API credentials are created from the admin portal, include client ID
  and client secret values for OAuth 2.0 client credentials, and the **Read
  All** permission gives GET access across API resources while **Read Users**
  is narrower and only covers User, Role, and Group resources.
- Ran the prose scan; it returned no banned filler or UI-arrow matches after
  patching.
- Ran `pnpm --filter @doow/docs check:content`; it passed with 43 docs pages
  and 43 search records.
- Rewrote `apps/docs/content/docs/integrations/identity/okta.mdx` as the Okta
  setup guide under the connection-source standard. The page now opens with
  users, groups, app assignments, and sign-in activity from an Okta org, names
  the dedicated API Services app requirement, keeps the existing
  client-credentials product contract, tightens read and non-read boundaries,
  makes event hooks conditional, and adds Okta-specific proof and recovery
  paths for missing users, groups, app assignments, sign-in activity, and event
  hook updates.
- Checked official Okta docs while editing provider-specific language: Okta
  API service integrations use OAuth 2.0 client credentials for service-to-
  service access, scoped Okta API tokens can include `okta.users.read` and
  `okta.groups.read`, the System Log API is read-only and supports sign-in
  events, and event hooks require endpoint verification before delivery.
- Ran the prose scan; it returned no banned filler or UI-arrow matches after
  patching.
- Ran `pnpm --filter @doow/docs check:content`; it passed with 43 docs pages
  and 43 search records.
- Rewrote `apps/docs/content/docs/integrations/identity/microsoft-365.mdx`
  as the Microsoft 365 setup guide under the connection-source standard. The
  page now uses Microsoft Entra ID naming, opens with users, groups, app
  assignments, and sign-in activity, names tenant admin consent as the setup
  owner requirement, adds a callout about tenant-wide Microsoft Graph
  application permissions, tightens read and non-read boundaries, and makes
  HRIS/usage next steps conditional.
- Checked official Microsoft Learn docs while editing provider-specific
  language: Microsoft Graph `User.Read.All` reads user profiles and requires
  admin consent, `AuditLog.Read.All` reads audit log activities and requires
  admin consent, the sign-ins API uses `AuditLog.Read.All` or
  `Directory.Read.All`, and application permissions always require admin
  consent without a signed-in user context.
- Ran the prose scan; it returned no banned filler or UI-arrow matches after
  patching.
- Ran `pnpm --filter @doow/docs check:content`; it passed with 43 docs pages
  and 43 search records.
- Rewrote `apps/docs/content/docs/integrations/identity/google-workspace.mdx`
  as the Google Workspace setup guide under the connection-source standard. The
  page now opens with users, groups, and login activity from the company
  directory, names the admin/OAuth approval requirement, adds a callout for
  Google Workspace app access controls, corrects the permissions table to
  include user, group, group member, and audit read scopes, tightens read and
  non-read boundaries, and makes HRIS/usage next steps conditional.
- Checked official Google Workspace docs while editing provider-specific
  language: the Admin SDK Directory API covers users, groups, and group
  members; Directory API read scopes include user, group, and group member read
  scopes; the Reports API audit read scope covers audit reports for the
  Workspace domain; Google Admin console app access controls can trust, limit,
  or block OAuth apps.
- Ran the prose scan; it returned no banned filler or UI-arrow matches after
  patching.
- Ran `pnpm --filter @doow/docs check:content`; it passed with 43 docs pages
  and 43 search records.
- Rewrote `apps/docs/content/docs/integrations/usage/cloud-platforms/index.mdx`
  as the Cloud platforms chooser under the connection-source standard. The page
  now explains when AWS/Azure/Google Cloud are the source of record, routes
  users to direct provider APIs, SDK, or observability when cloud data is the
  wrong path, compares AWS Bedrock, Azure OpenAI, and Google Vertex AI by
  source and setup owner, defines read and non-read boundaries, and points to
  exact setup guides.
- Ran the prose scan; it returned no banned filler or UI-arrow matches after
  patching.
- Ran `pnpm --filter @doow/docs check:content`; it passed with 43 docs pages
  and 43 search records.
- Rewrote `apps/docs/content/docs/integrations/usage/observability/otlp.mdx`
  as the OTLP push setup guide under the connection-source standard. The page
  now opens with when to use OTLP push versus the Instrumentation SDK, names
  collector owner requirements, keeps the Doow OTLP/HTTP exporter config, adds
  a "send only the metrics Doow needs" callout, frames Doow reads around
  metric names, delta data points, resource attributes, and timestamps, and
  adds disconnecting plus production checks for secrets, pipeline scope,
  retries, attribution, and temporality.
- Checked official OpenTelemetry docs while editing OTLP-specific language:
  OTLP exporter configuration supports endpoint, header, timeout, protocol,
  and compression settings; OTLP/HTTP metrics use signal-specific metric
  endpoints in standard exporter configuration; the metrics data model includes
  delta and cumulative aggregation temporality.
- Ran the prose scan; it returned no banned filler or UI-arrow matches after
  patching.
- Ran `pnpm --filter @doow/docs check:content`; it passed with 43 docs pages
  and 43 search records.
- Rewrote `apps/docs/content/docs/integrations/usage/observability/sentry.mdx`
  as the Sentry setup guide under the connection-source standard. The page now
  opens with event-count and outcome status signals from a Sentry organization,
  names the organization slug and OAuth authorization requirements, adds a
  reporting-safe authorization callout, frames Doow reads around event counts,
  outcome status, category, project, and time windows, tightens proof and
  troubleshooting, and makes identity/HRIS next steps conditional.
- Checked official Sentry docs while editing provider-specific language:
  Sentry's organization stats endpoint queries event counts and supports
  grouping/filtering by fields such as outcome, category, reason, and project;
  Sentry OAuth grants third-party applications access to the organization
  selected during authorization; Sentry organization slugs appear in
  organization URLs and organization settings.
- Ran the prose scan; it returned no banned filler or UI-arrow matches after
  patching.
- Ran `pnpm --filter @doow/docs check:content`; it passed with 43 docs pages
  and 43 search records.
- Rewrote `apps/docs/content/docs/integrations/usage/observability/appsignal.mdx`
  as the AppSignal setup guide under the connection-source standard. The page
  now opens with metric timeseries that already exist for one AppSignal app,
  names App ID and Personal API token requirements, adds a reporting-safe token
  callout, frames Doow reads around app-level metric timeseries, environment,
  namespace, tags, and time windows, tightens proof and troubleshooting, and
  makes identity/HRIS next steps conditional.
- Checked official AppSignal docs while editing provider-specific language:
  AppSignal API requests require a personal API token and app-scoped endpoints
  require an application ID; the GraphQL API can query application data
  including metrics. Kept incidents, exception messages, stack traces, session
  data, request payloads, source code, environment configuration, and billing
  payment details out of scope.
- Ran the prose scan; it returned no banned filler or UI-arrow matches after
  patching.
- Ran `pnpm --filter @doow/docs check:content`; it passed with 43 docs pages
  and 43 search records.
- Rewrote `apps/docs/content/docs/integrations/usage/observability/newrelic.mdx`
  as the New Relic setup guide under the connection-source standard. The page
  now opens with metric data or event counts that already exist in New Relic,
  names Account ID and User API key requirements, adds a reporting-safe User
  API key callout, frames Doow reads around configured metric or NRQL queries,
  event counts, attributes, and time windows, tightens proof and
  troubleshooting, and makes identity/HRIS next steps conditional.
- Checked official New Relic docs while editing provider-specific language:
  New Relic user keys are used for NerdGraph and New Relic APIs, NerdGraph is
  the GraphQL API for querying data and configuration, and New Relic metric
  data can be queried through NRQL and NerdGraph. Kept traces, logs, browser
  session data, source code, secrets, and billing payment details out of scope.
- Ran the prose scan; it returned no banned filler or UI-arrow matches after
  patching.
- Ran `pnpm --filter @doow/docs check:content`; it passed with 43 docs pages
  and 43 search records.
- Rewrote `apps/docs/content/docs/integrations/usage/observability/datadog.mdx`
  as the Datadog setup guide under the connection-source standard. The page now
  opens with usage metrics and event counts that already exist in Datadog,
  names Datadog site, API key, Application key, or OAuth requirements, adds a
  reporting-safe key-pair callout, frames Doow reads around configured metric
  queries, event counts, tags, and time windows, tightens proof and
  troubleshooting, and makes identity/HRIS next steps conditional.
- Checked official Datadog docs while editing provider-specific language:
  Datadog documents API keys and Application keys as separate API access
  credentials, and the Metrics API exposes metric metadata/query surfaces.
  Kept the page focused on metrics/event counts rather than traces, logs,
  source code, secrets, or billing payment details.
- Ran the prose scan; it returned no banned filler or UI-arrow matches after
  patching.
- Ran `pnpm --filter @doow/docs check:content`; it passed with 43 docs pages
  and 43 search records.
- Rewrote `apps/docs/content/docs/integrations/usage/observability/index.mdx`
  as the Observability overview and chooser under the connection-source
  standard. The page now opens with the right source decision, explains when
  observability is not the source of record, separates platform pull from OTLP
  push, lists Datadog/New Relic/AppSignal/Sentry setup paths, defines what Doow
  reads for platform pull and OTLP push, keeps traces/logs/payloads/secrets out
  of scope, and routes readers to the exact setup page.
- Ran the prose scan; it returned no banned filler or stale wording after
  removing the remaining UI-arrow match.
- Ran `pnpm --filter @doow/docs check:content`; it passed with 43 docs pages
  and 43 search records.
- Rewrote `apps/docs/content/docs/integrations/usage/instrumentation-sdk/cli.mdx`
  as the CLI / Daemon setup guide under the connection-source standard. The
  page now opens with scripts/jobs/batch workers as the reader job, adds
  prerequisites and a one-pipe proof callout, preserves install, pipe mode,
  daemon mode, config, and SIGHUP examples, adds an event privacy boundary, and
  makes production readiness explicit around secrets, delivery failure logs,
  daemon logs, environment keys, metric stability, and shutdown time.
- Ran the prose scan; it returned no banned filler, stale wording, or
  over-claimed CLI/daemon behavior after patching.
- Ran `pnpm --filter @doow/docs check:content`; it passed with 43 docs pages
  and 43 search records.
- Rewrote `apps/docs/content/docs/integrations/usage/instrumentation-sdk/serverless.mdx`
  as the Serverless setup guide under the connection-source standard. The page
  now opens with the serverless-specific reader job, adds prerequisites and a
  flush-proof callout, preserves Lambda/Vercel/Azure examples while removing
  unverified wrapper internals, adds an event-shape privacy boundary, and makes
  production readiness explicit around secrets, runtime flush behavior,
  retries, timeouts, environment keys, and metric stability.
- Ran the prose scan; it returned no banned filler, stale wording, or
  over-claimed wrapper behavior after patching.
- Ran `pnpm --filter @doow/docs check:content`; it passed with 43 docs pages
  and 43 search records.
- Rewrote `apps/docs/content/docs/integrations/usage/instrumentation-sdk/sidecar.mdx`
  as the Sidecar setup guide under the connection-source standard. The page
  now opens with the right reader job, adds prerequisites before input modes,
  explains why to keep the sidecar beside the app, preserves TCP/stdin/file
  tail configuration, adds the event privacy boundary, tightens health-check,
  verification, production, troubleshooting, and shutdown guidance, and avoids
  unverified runtime guarantees that are not backed by code in this monorepo.
- Ran the prose scan; it returned no banned filler, stale wording, or
  over-claimed sidecar behavior after patching.
- Ran `pnpm --filter @doow/docs check:content`; it passed with 43 docs pages
  and 43 search records.
- Rewrote `apps/docs/content/docs/integrations/usage/instrumentation-sdk/nodejs.mdx`
  as the Node.js SDK setup guide under the connection-source standard. The
  page now opens with the exact reader job, adds prerequisites before install,
  moves the reader through one test event before broad instrumentation, defines
  the event contract with privacy boundaries, removes unverified certainty such
  as "SDK never throws," keeps options/env vars as reference-like support, and
  makes production readiness explicit around secrets, batching, retry,
  shutdown, environment keys, and metric stability.
- Checked local repo references before editing. The SDK surface still appears
  docs-only in this monorepo, so the guide preserved existing documented API
  names but avoided adding new behavior claims.
- Ran `pnpm --filter @doow/docs check:content`; it passed with 43 docs pages
  and 43 search records.
- Rewrote `apps/docs/content/docs/integrations/usage/instrumentation-sdk/index.mdx`
  as the Instrumentation SDK overview and chooser under the connection-source
  standard. The page now explains when the SDK is the right source, tells users
  to prefer provider data when it already records the usage, compares Node.js
  SDK, Sidecar, Serverless, and CLI/Daemon methods once instead of duplicating
  tables, defines the event contract, warns against sending payloads or
  secrets as metadata, and ends with runtime-specific setup links.
- Checked the local repo for SDK implementation signals before editing. The
  current monorepo only contains docs references for `@doow/track`,
  `doow/track-sidecar`, and `doow-track`, so the overview stayed conservative
  and did not add new runtime-specific behavior claims.
- Ran `pnpm --filter @doow/docs check:content`; it passed with 43 docs pages
  and 43 search records.
- Rewrote `apps/docs/content/docs/integrations/usage/cloud-platforms/google-vertex.mdx`
  as the Google Vertex AI setup guide under the connection-source standard. The
  page now opens with usage, cost, project, SKU, and region signals from Google
  Cloud billing data, frames the integration as a Cloud Billing export to
  BigQuery path, adds BigQuery dataset and job-permission requirements, keeps
  Billing Account Viewer as approval-context access, clarifies project filters,
  and makes identity/HRIS next steps conditional.
- Checked official Google Cloud docs while editing provider-specific language:
  Cloud Billing export to BigQuery contains services, SKUs, projects, labels,
  locations, cost, usage, credits, adjustments, and currency; billing exports
  collect data from setup time forward; Billing Account Viewer is the
  read-only billing visibility role; querying export data requires BigQuery
  dataset and job access. Kept model request payloads, endpoint configuration,
  and payment details out of Doow reads.
- Ran `pnpm --filter @doow/docs check:content`; it passed with 43 docs pages
  and 43 search records.
- Rewrote `apps/docs/content/docs/integrations/usage/cloud-platforms/azure-openai.mdx`
  as the Azure OpenAI setup guide under the connection-source standard. The
  page now opens with token usage, deployment activity, and model usage by
  Azure subscription/resource/region, uses Microsoft Entra ID and Monitoring
  Reader wording, adds a narrow-scope read-only callout, uses Azure Monitor
  metric names such as `ProcessedPromptTokens`, `GeneratedTokens`,
  `TokenTransaction`, `InputTokens`, `OutputTokens`, and `TotalTokens`, removes
  billing-data overreach, and makes identity/HRIS next steps conditional.
- Checked official Microsoft docs while editing provider-specific language:
  Azure OpenAI metrics are exposed under Microsoft.CognitiveServices/accounts
  with deployment/model/region dimensions, and Azure Monitor's Monitoring
  Reader role can query Azure Monitor metrics at assigned resource, resource
  group, or subscription scope. Kept the page focused on metrics rather than
  deployment configuration or billing access.
- Ran `pnpm --filter @doow/docs check:content`; it passed with 43 docs pages
  and 43 search records.
- Rewrote `apps/docs/content/docs/integrations/usage/cloud-platforms/aws-bedrock.mdx`
  as the AWS Bedrock setup guide under the connection-source standard. The page
  now opens with token usage, invocation activity, and model usage by AWS
  account/region, removes over-broad billing-signal language, adds a dedicated
  read-only CloudWatch credential callout, uses AWS metric names
  `InputTokenCount` and `OutputTokenCount`, clarifies multi-region and
  multi-account setup, and makes identity/HRIS next steps conditional.
- Checked official AWS docs while editing provider-specific language:
  Amazon Bedrock publishes runtime metrics through CloudWatch, including
  `InputTokenCount` and `OutputTokenCount`; CloudWatch supports
  `GetMetricData` and `ListMetrics` as read/list permissions. Kept the policy
  focused on CloudWatch metric reads rather than adding unverified Bedrock or
  billing permissions.
- Ran `pnpm --filter @doow/docs check:content`; it passed with 43 docs pages
  and 43 search records.
- Rewrote `apps/docs/content/docs/integrations/usage/direct-provider-apis/authress.mdx`
  as the Authress setup guide under the connection-source standard. The page
  now opens with API call usage, authorization activity, and
  credential-operation outcomes, names service client access key and API URL
  requirements, adds a dedicated service-client trust note, frames Doow reads
  around usage and event metadata, and makes identity/accounting next steps
  conditional.
- Checked official Authress docs while editing provider-specific language:
  Authress uses service clients and access keys for programmatic callers,
  documents billable routes and `/v1/accounts/{accountId}/usage`, and supports
  audit streams for authentication and authorization events. Kept policy
  content, user profiles, and payment details out of Doow reads.
- Ran `pnpm --filter @doow/docs check:content`; it passed with 43 docs pages
  and 43 search records.
- Rewrote `apps/docs/content/docs/integrations/usage/direct-provider-apis/replicate.mdx`
  as the Replicate setup guide under the connection-source standard. The page
  now opens with prediction counts, model activity, and compute usage outcomes,
  names account/organization token scope, adds a dedicated-token callout,
  frames Doow reads around prediction metadata and runtime metrics, keeps
  prediction inputs/outputs and model weights out of scope, and makes
  identity/accounting next steps conditional.
- Checked official Replicate docs while editing provider-specific language:
  API tokens authenticate HTTP API requests, prediction objects include model,
  status, timestamps, and runtime metrics such as `predict_time`, list
  predictions returns predictions for the user or organization associated with
  the token, and Replicate billing is based on compute time or input/output
  pricing depending on the model.
- Ran `pnpm --filter @doow/docs check:content`; it passed with 43 docs pages
  and 43 search records.
- Rewrote `apps/docs/content/docs/integrations/usage/direct-provider-apis/openrouter.mdx`
  as the OpenRouter setup guide under the connection-source standard. The page
  now opens with the OpenRouter usage outcome, uses current management-key
  wording, distinguishes organization-wide usage from normal API-key metadata,
  tightens the usage/spend/token fields, keeps prompt/completion content
  boundaries, and makes identity/accounting next steps conditional.
- Checked official OpenRouter docs while editing provider-specific language:
  management keys are for administrative key operations and cannot call
  completion endpoints; list-key endpoints require a management key and return
  usage fields; activity exports can group spend, tokens, and requests by
  model, API key, or creator; usage accounting is also returned with model API
  responses. Avoided the old provisioning-key wording except where the API
  still exposes legacy flags.
- Ran `pnpm --filter @doow/docs check:content`; it passed with 43 docs pages
  and 43 search records.
- Rewrote `apps/docs/content/docs/integrations/usage/direct-provider-apis/fireworks-ai.mdx`
  as the Fireworks AI setup guide under the connection-source standard. The
  page now opens with the Fireworks usage outcome, names account, API key, and
  usage/billing/metrics access up front, adds a reporting-safe key callout,
  keeps prompt/completion content boundaries, tightens proof/troubleshooting,
  and makes identity/accounting next steps conditional.
- Checked official Fireworks docs while editing provider-specific language:
  Fireworks documents API key creation, billing metrics export for account-wide
  usage, and Prometheus-style deployment metrics. Kept the page conservative
  instead of promising one universal Fireworks usage surface.
- Ran `pnpm --filter @doow/docs check:content`; it passed with 43 docs pages
  and 43 search records.
- Rewrote `apps/docs/content/docs/integrations/usage/direct-provider-apis/groq.mdx`
  as the Groq setup guide under the connection-source standard. The page now
  opens with the Groq usage outcome, names GroqCloud Console, Prometheus
  Metrics, and API key access up front, removes stale model examples, keeps
  prompt/completion content boundaries, tightens proof/troubleshooting, and
  makes identity/accounting next steps conditional.
- Checked official Groq docs while editing provider-specific language:
  Groq's docs show API Keys in the console and document Prometheus Metrics as
  an Enterprise feature using API-key bearer authentication. Added the metrics
  access caveat so customers are not surprised during setup.
- Ran `pnpm --filter @doow/docs check:content`; it passed with 43 docs pages
  and 43 search records.
- Rewrote `apps/docs/content/docs/integrations/usage/direct-provider-apis/anthropic.mdx`
  as the Anthropic setup guide under the connection-source standard. The page
  now opens with organization-level Claude usage outcomes, uses Admin API key
  wording for Anthropic's Usage & Cost Admin API, removes stale model examples,
  keeps prompt/completion content boundaries, tightens proof/troubleshooting,
  and makes identity/accounting next steps conditional.
- Checked official Anthropic docs while editing provider-specific language:
  Usage & Cost API docs say usage reports require an Admin API key and only
  organization admins can provision those keys through Console. Kept cost
  wording out of Doow reads because the page only promises usage metrics.
- Ran `pnpm --filter @doow/docs check:content`; it passed with 43 docs pages
  and 43 search records.
- Rewrote `apps/docs/content/docs/integrations/usage/direct-provider-apis/openai.mdx`
  as the OpenAI setup guide under the new connection-source standard. The page
  now opens with the OpenAI usage outcome, narrows Admin API key wording to
  usage reporting and minimum access, removes dated model examples, keeps
  prompt/completion content boundaries, tightens proof/troubleshooting, and
  makes identity/accounting next steps conditional.
- Checked official OpenAI docs while editing provider-specific language:
  Usage API examples use `OPENAI_ADMIN_KEY`, and Admin API key docs describe
  organization-level admin keys. Kept the page conservative rather than
  guessing UI labels or exact role names.
- Ran `pnpm --filter @doow/docs check:content`; it passed with 43 docs pages
  and 43 search records.
- Rewrote `apps/docs/content/docs/integrations/usage/direct-provider-apis/index.mdx`
  as a source-family overview. It now frames direct provider APIs as the path
  for provider-of-record usage with read-only credentials, names provider admin
  ownership, adds pre-choice readiness checks, separates AI provider versus
  cloud platform paths, and adds common blockers before the individual setup
  pages.
- Ran `pnpm --filter @doow/docs check:content`; it passed with 43 docs pages
  and 43 search records.
- Rewrote `apps/docs/content/docs/integrations/usage/doow-agent/desktop-agent.mdx`
  to match the Browser Agent quality bar. It now opens with the native desktop
  usage outcome, clarifies when Desktop Agent is right, tightens rollout
  owner/platform/privacy readiness, preserves the desktop privacy boundary and
  platform availability table, and makes next steps conditional on missing
  context.
- Ran `pnpm --filter @doow/docs check:content`; it passed with 43 docs pages
  and 43 search records.
- Rewrote `apps/docs/content/docs/integrations/usage/doow-agent/browser-agent.mdx`
  as a practical Browser Agent rollout guide. It now opens with the usage
  outcome, clarifies when provider APIs are enough, links Desktop Agent
  directly for native app usage, tightens rollout owner/browser-permission
  readiness, states the browser-content trust boundary, and keeps proof plus
  recovery checks.
- Ran `pnpm --filter @doow/docs check:content`; it passed with 43 docs pages
  and 43 search records.
- Rewrote `apps/docs/content/docs/integrations/usage/doow-agent/index.mdx`
  as the Doow Agent family overview. It now frames Doow Agent as the path for
  usage that happens on employee devices, states Browser Agent and Desktop
  Agent are one usage-collection product across device surfaces, tightens
  rollout/privacy/proof language, and adds identity as a conditional next
  source when agent activity needs user/group ownership.
- Ran `pnpm --filter @doow/docs check:content`; it passed with 43 docs pages
  and 43 search records.
- Rewrote `apps/docs/content/docs/integrations/usage/choose-source.mdx` as a
  usage-source decision guide. It now chooses by where activity is recorded,
  who controls setup, and what proof Doow should show. The old "recommended
  default" section is now conditional "common choices," and the comparison
  table includes likely setup owners instead of only examples.
- Ran `pnpm --filter @doow/docs check:content`; it passed with 43 docs pages
  and 43 search records.
- Rewrote `apps/docs/content/docs/integrations/connect.mdx` as the common
  connection-flow guide. The page now starts after source choice, links to
  "Choose what to connect," keeps provider flow details intact, removes the
  fixed usage-first sequence, cleans up old em-dash/click/you-can prose, and
  makes next steps conditional on missing context.
- Ran `pnpm --filter @doow/docs check:content`; it passed with 43 docs pages
  and 43 search records.
- Rewrote `apps/docs/content/docs/integrations/how-integrations-work.mdx` as a
  tighter explanation page. It now defines an integration as a source
  connection, clarifies validation and first-sync behavior, strengthens proof
  language, keeps pause/disconnect/revoke guidance, and links back to "Choose
  what to connect."
- Ran `pnpm --filter @doow/docs check:content`; it passed with 43 docs pages
  and 43 search records.
- Rewrote `apps/docs/content/docs/integrations/choose-what-to-connect-first.mdx`
  as "Choose what to connect" while keeping the existing route to avoid link
  churn. The page now serves first-time and returning customers, removes
  "first integration" framing, routes by missing data/outcome, and keeps
  baseline recommendations non-mandatory.
- Updated the matching sidebar item in `apps/docs/src/lib/docs/navigation.ts`
  from "Choose what to connect first" to "Choose what to connect."
- Ran `pnpm --filter @doow/docs check:content`; it passed with 43 docs pages
  and 43 search records. Ran `pnpm --filter @doow/docs build`; it passed.
- Rewrote `apps/docs/content/docs/integrations/what-doow-needs.mdx` as an
  explanation page for Doow's three data layers. It now removes mandatory
  order language, presents usage, organization, and financial sources as
  question-driven context, and routes readers to choosing or connecting based
  on whether they already know the source.
- Ran `pnpm --filter @doow/docs check:content`; it passed with 43 docs pages
  and 43 search records.
- Rewrote `apps/docs/content/docs/integrations/index.mdx` as the first
  page-by-page integrations docs sweep. The page now frames integrations as
  connection-source choices, removes the fixed first-setup sequence, routes by
  customer outcome, and keeps success verification near the bottom.
- Ran `pnpm --filter @doow/docs check:content`; it passed with 43 docs pages
  and 43 search records.

## Next

- Continue the sweep with
  `apps/docs/content/docs/integrations/usage/observability/sentry.mdx`,
  checking credential wording, what Doow reads, proof, troubleshooting, and
  conditional next steps.
