# 2026-05-09 — doow-web

## 2026-05-09T13:30Z | [doow-web] | [Codex]

## Done

- [doow-web] Refined the docs header, top navigation, sidebar, homepage density,
  and article layout after reviewing Clerk, Stytch, Cloudflare, and TanStack
  docs. The new direction uses a compact command header, product-area nav,
  sidebar search trigger, tighter spacing, smaller navigation type, calmer
  card treatment, and stronger active states.
- [doow-web] Renamed the docs navigation model from generic "Guides/Changelog"
  to "Workflows/Resources" in the top nav and sidebar grouping.

## Verified

- [doow-web] Passed `pnpm --filter @doow/docs typecheck`,
  `pnpm --filter @doow/docs lint`, `pnpm --filter @doow/docs check:content`,
  and `pnpm --filter @doow/docs build`.
- [doow-web] Browser-checked the docs homepage and quickstart page on
  `http://localhost:3001`; the header, sidebar, hero, and right rail render.

## 2026-05-09T13:08Z | [doow-web] | [Codex]

## Done

- [doow-web] Created `packages/ui` as a shared package candidate for Doow UI
  primitives, system components, `cn`, `foundation.css`, and `system.css`.
- [doow-web] Rolled back web consumption of `@doow/ui` after browser testing
  showed the landing page regressed. Root cause: moving component class strings
  outside the web app changed Tailwind's source scanning behavior. Web now
  owns its original local components and CSS again.
- [doow-web] Kept `@doow/ui` wired to docs only for the next docs token/component
  migration pass, and added an explicit Tailwind `@source` for the shared UI
  package in docs CSS.

## Verified

- [doow-web] Browser checked the web app at `http://localhost:3000` after the
  rollback; landing page nav, hero copy, hero buttons, and media styling render
  again. The earlier `3000` false-positive came from another Doow worktree
  server, which was stopped before retesting this repo.

## Next

- [doow-web] Before web consumes `@doow/ui`, add a package-consumption proof:
  Tailwind source registration, production CSS diff/screenshot comparison, and
  explicit browser snapshots for `/`, `/pricing`, `/blog`, and `/signin`.

## Done
- Compared docs at localhost:3001 against Clerk / Firecrawl / Ramp / Claude docs; identified 11 gaps (no dark mode, flat sidebar, marketing-flavored hero, no AI affordances, etc.)
- Wrote A→Z plan at `~/.claude/plans/plan-all-a-z-then-breezy-wozniak.md` and shipped phases A–H end-to-end on the `animations` branch:
  - Phase A: dark mode (`:root.dark` token block, `ThemeScript` blocking init, `ThemeToggle`, dual-theme rehype-pretty-code)
  - Phase B: grouped sidebar IA, 16 new MDX stub pages, NEW/BETA badge styling
  - Phase C: dev-first hero with curl code card; replaced "Choose your path" with "From idea to production" stepped journey + SDK row + utility row
  - Phase D: interactive `<Tabs>` (keyboard nav + localStorage persistence via `useSyncExternalStore`); `prerequisites` + `nextSteps` frontmatter fields
  - Phase E: `AskDocsButton` (replaces disabled "Ask AI"), GitHub icon (env-gated), platform-aware ⌘K/Ctrl K
  - Phase F: `IntersectionObserver` ToC active highlight; right-rail CTA card with accent strip; tamed H1 from 5.2rem → 3rem
  - Phase G: `/llms.txt` manifest, `/raw/[slug]` raw-MDX route, `PageActions` menu (Copy / Open in ChatGPT / Open in Claude / View raw)
- Full `pnpm verify` green: boundaries, deployment, lint, tests, content gate (20/20 pages), build (46 static pages).

## In Progress
- User flagged the result reads "vibecoded" — confirmed the root cause:
  1. Docs invented its own `--docs-*` token vocabulary, ignoring the audited Doow tokens (`--bg-*`, `--fg-*`, `--button-*`, the full `--blog-*` set with 4 callout tones, the `--type-doow-*` Figma scale)
  2. Did **not** use shadcn `apps/web/src/components/ui/*` (button, dialog, input, tooltip, skeleton) or Doow `apps/web/src/components/system/*` (badge, card, container, cta-group, section-heading, etc.) — built one-off primitives instead. `@radix-ui/react-slot` + `clsx` are already in docs deps for this purpose, unused.
- No code touched after the user pushback — waiting on confirmation of the cleanup approach.

## Next
- Decide cleanup scope with user. Recommended sequence:
  1. **Token reconciliation first** — replace `--docs-*` block with the Doow token vocabulary (`--bg-*`, `--fg-*`, `--blog-*`, `--type-doow-*`) so everything downstream snaps to brand for free.
  2. **Promote `ui/` + `system/` to `packages/ui`** — per `CLAUDE.md` "extract to packages when both apps need it"; both now do.
  3. **Component-by-component port**, in order of leverage: Callout (use the 4-tone `--blog-callout-*` set), Article header + ToC + PageActions, CodeBlock, Sidebar, Hero/landing.
- Open question: should we promote tokens to `packages/design-tokens` (currently a 3-color stub) or duplicate into `apps/docs/src/styles/tokens/foundation.css`? Promotion is the right call long-term but expands scope — flag for user.

## Notes
- Branch is still `animations`. Lots of uncommitted work: 12 modified files across `apps/docs/src/{app,components,lib,styles}/` and 16 new MDX stubs under `apps/docs/content/docs/`. No commits yet.
- Brand black is `#011313` (Doow `--fg-base`), not `#071513` like the docs `--docs-text` uses. Radii in docs are `0.5/0.75/1/1.5rem`, web is `0.5/1/1.5/2rem`. These are the kind of "almost-aligned-but-not" mismatches that cause the off-brand feel.
- Web blog has working `apps/web/src/components/blog/{article-layout,article-header,table-of-contents,mdx-components,search-bar,related-posts,pagination}.tsx` — the docs should port from these rather than reinvent.
- shadcn primitives in `apps/web/src/components/ui/`: `button`, `dialog`, `input`, `skeleton`, `tooltip`.
- Doow system components in `apps/web/src/components/system/`: `badge`, `button`, `card`, `container`, `cta-group`, `footer-list`, `form-field`, `input`, `media-frame`, `nav-link`, `section-heading`.
