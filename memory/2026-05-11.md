# 2026-05-11

## 2026-05-11T14:49Z | [doow-web] | [Codex]

- Replaced the docs MDX author-facing components that overlap with Stytch's
  patterns with shared shadcn-style primitives.
- Added `@doow/ui` primitives for `Card`, `Alert`, and Radix-backed `Tabs`,
  exported them from the shared UI package, and added
  `@radix-ui/react-tabs`.
- Updated docs `Callout`, `Card`, `Steps`, `Tooltip`, and `Tabs` rendering so
  MDX keeps the same author API while composing shared UI primitives instead
  of raw local shells.
- Preserved the existing docs visual treatment for cards, callouts, and tabs,
  added a numbered rail treatment for `Steps`, then checked `/integrations`
  and `/integrations/choose-what-to-connect-first` visually in the browser.
- Verified `pnpm --filter @doow/ui typecheck`,
  `pnpm --filter @doow/docs typecheck`,
  `pnpm --filter @doow/docs check:content`,
  `pnpm --filter @doow/docs lint`, and
  `pnpm --filter @doow/docs build`; all passed.

## 2026-05-11T13:37Z | [doow-web] | [Codex]

- Implemented the Stytch-quality integrations docs pass on top of the earlier
  guide-first restructure.
- Added `docs/rebuild/integrations-docs-stytch-quality-plan.md` to capture the
  production plan, open backend questions, and quality gates.
- Reworked the `/integrations` front door and priority choice pages with
  decision cards, callouts, proof states, and clearer task progression.
- Added `/integrations/usage/cloud-platforms` so Cloud platforms is a real
  overview route instead of pointing directly to AWS Bedrock.
- Strengthened priority provider guides with concrete confirmation steps,
  symptom-based troubleshooting, and production checklists.
- Fixed MDX card styling in the docs app so card links keep the intended
  neutral body text instead of inheriting green underlined prose-link styling.
- Re-ran `pnpm --filter @doow/docs check:content`,
  `pnpm --filter @doow/docs typecheck`, `pnpm --filter @doow/docs lint`, and
  `pnpm --filter @doow/docs build`; all passed. Checked the new docs routes and
  refreshed the local `/integrations` page visually.

## 2026-05-11T12:57Z | [doow-web] | [Codex]

- Implemented the guide-first integrations docs direction.
- Rewrote `/integrations` as a decision front door for confused users who do
  not yet know what Doow needs or what to connect first.
- Added guide pages for `what-doow-needs`, `choose-what-to-connect-first`,
  `how-integrations-work`, and `usage/choose-source`.
- Updated docs navigation so the sidebar starts with Start here, then Usage
  data, Organization data, and Financial data.
- Added confirmation, troubleshooting, and next-step guidance across the
  imported setup pages so provider pages behave more like task-completion
  guides.
- Verified `pnpm --filter @doow/docs check:content`,
  `pnpm --filter @doow/docs typecheck`, `pnpm --filter @doow/docs lint`, and
  `pnpm --filter @doow/docs build`; all passed. Local route checks for the new
  guide pages returned 200.

## 2026-05-11T12:31Z | [doow-web] | [Codex]

- Updated `docs/rebuild/integrations-docs-content-readiness-audit.md` with
  the refined guide-first direction.
- Captured that users may arrive confused about what Doow needs or what to
  connect first, so `/integrations` must orient before showing the provider
  catalog.
- Added the target guide structure: Start here, Usage data, Organization data,
  and Financial data.
- Updated the implementation order to start with the front-door decision guide,
  recommended first setup, and usage-data choice guide before provider page
  rewrites.

## 2026-05-11T10:55Z | [doow-web] | [Codex]

- Added `docs/rebuild/integrations-docs-content-readiness-audit.md` as a
  planning-only audit for the imported integrations docs content.
- Captured the current content tree, readiness verdict, P0/P1/P2 issues,
  recommended provider and SDK page templates, and implementation order.
- Did not edit live docs MDX pages or UI code as requested.

## 2026-05-11T10:29Z | [doow-web] | [Codex]

- Updated the docs sidebar IA to reflect the backend-provided integrations
  source hierarchy instead of flattening every provider into sibling groups.
- Added nested sidebar support for parent links and label-only subgroups, so
  Usage data contains Direct provider APIs, Instrumentation SDK, and
  Observability; Direct provider APIs contains AI providers and Cloud
  platforms; Organization data contains Identity providers and HRIS; Financial
  data contains Accounting.
- Replaced the remaining dummy sidebar card copy and docs metadata that still
  referenced setup, workflows, API, or releases.
- Verified `pnpm --filter @doow/docs check:content`,
  `pnpm --filter @doow/docs typecheck`, `pnpm --filter @doow/docs lint`, and
  `pnpm --filter @doow/docs build`; all passed. Refreshed the local docs page
  and checked the nested sidebar visually.

## 2026-05-11T10:18Z | [doow-web] | [Codex]

- Removed the old scaffold docs content so the docs corpus now contains only
  the legitimate backend-provided integrations pages.
- Deleted 20 placeholder MDX pages for getting started, workflows, reference,
  changelog, and related dummy routes.
- Pruned docs navigation, sidebar, search copy, article footer links, sitemap
  priority logic, and `/start` redirect so they no longer point to removed
  dummy pages.
- Changed the docs root route to redirect to `/integrations`.
- Verified `pnpm --filter @doow/docs check:content`,
  `pnpm --filter @doow/docs typecheck`, `pnpm --filter @doow/docs lint`, and
  `pnpm --filter @doow/docs build`; all passed. The content gate now reports
  35 pages and 35 search records.

## 2026-05-11T09:47Z | [doow-web] | [Codex]

- Imported the backend-provided integrations bundle from
  `/home/ayodele/Downloads/integrations` into the docs app as 35 published MDX
  pages under `apps/docs/content/docs/integrations`.
- Added directory-index docs support so `/integrations`,
  `/integrations/hris`, and other nested overview routes can live as
  `index.mdx` pages without flattening the IA.
- Added the Integrations top-nav item and sidebar groups for direct provider
  APIs, cloud platforms, the instrumentation SDK, observability, identity
  providers, HRIS, and accounting.
- Updated `connect-data` to link into the new integration hub and normalized
  public terminology around usage-based integrations, direct provider APIs,
  instrumentation SDK, and observability integrations.
- Verified with `pnpm --filter @doow/docs check:content`,
  `pnpm --filter @doow/docs typecheck`, `pnpm --filter @doow/docs lint`, and
  `pnpm --filter @doow/docs build`; all passed.

## 2026-05-11T09:12Z | [doow-web] | [Codex]

- Implemented the first step for production integrations docs: nested docs
  route and content-loader support.
- Replaced single-segment docs routes with catch-all routes:
  `apps/docs/src/app/[...slug]/page.tsx` and
  `apps/docs/src/app/raw/[...slug]/route.ts`.
- Updated docs content loading to recurse through `content/docs/**/*.mdx` and
  validate nested filenames against slash-separated frontmatter slugs.
- Added path helpers for normalized docs slugs and route segments, updated the
  raw route and delete-page script to use them, and added `integrations` as a
  valid docs section.
- Verified existing flat pages still work through the catch-all route:
  `/getting-started` and `/raw/getting-started` returned `200`.
- Ran `pnpm --filter @doow/docs check:content`,
  `pnpm --filter @doow/docs typecheck`, `pnpm --filter @doow/docs lint`, and
  `pnpm --filter @doow/docs build`; all passed.

## 2026-05-11T08:01Z | [doow-web] | [Codex]

- Reproduced the remaining docs scroll-layer issue against Clerk's scrolled
  page using Peruz `elementsFromPoint` stacks.
- Found the real culprit: the visible `.docs-shell` frame and
  `.docs-shell__body` surface were still scrolling behind the fixed header and
  sidebar, which made it look like another full page was moving underneath the
  chrome.
- Moved the visible rounded frame to a fixed `.docs-shell::before` backplate
  and made `.docs-shell`, `.docs-main-pane`, and `.docs-shell__body`
  transparent on desktop.
- Added fixed top and bottom canvas masks so scrolled content can't bleed into
  the frame margin above the header or below the frame.
- Added dark-mode-specific overrides because `:root.dark .docs-shell__body`
  had higher specificity and was repainting the moving surface.
- Verified the scrolled dark screenshot at
  `/tmp/surf-snap-1778483332621.png`, then re-ran
  `pnpm --filter @doow/docs typecheck` and
  `pnpm --filter @doow/docs lint`.

## 2026-05-11T07:37Z | [doow-web] | [Codex]

- Reproduced the docs scroll issue in Peruz and compared it against
  `https://clerk.com/docs` at the same scrolled state.
- Found the scroll-specific culprit: Doow's docs content filled the whole
  remaining column from the sidebar edge, while Clerk centers and caps the
  scrolling `main` content around `1024px`. That gutter keeps page content from
  visually scraping under the fixed header and sidebar seams.
- Updated `apps/docs/src/styles/docs-layout-frame.css` so `.docs-home` and
  `.docs-empty` are centered at `64rem` on desktop, with `.docs-article`
  capped at `68rem`.
- Verified the scrolled geometry: the docs content now starts at `x=552` with
  a `1024px` width, and seam hit-tests return the shell/background gutter
  instead of page cards or text.
- Re-ran `pnpm --filter @doow/docs typecheck` and
  `pnpm --filter @doow/docs lint`.

## 2026-05-11T06:22Z | [doow-web] | [Codex]

- Re-checked `https://clerk.com/docs` in an isolated Peruz browser session and
  measured the live layout: the header is fixed across the top frame, the
  sidebar starts below the header, and the content is offset rather than
  placed under the chrome.
- Confirmed `clerk/clerk-docs` is open source for content only; the repository
  README says the docs site source code is not open source yet.
- Updated `apps/docs/src/styles/docs-layout-frame.css` so desktop docs use the
  same structural model: full-width framed header, sidebar fixed below it,
  full-height sidebar rail, and content padding reserved for both chrome
  regions.
- Moved the desktop brand presentation into the header through CSS overrides
  and hid the duplicate sidebar brand on desktop.
- Verified local geometry at `http://localhost:3001`: header `x=7 y=7 w=1825
  h=68`, sidebar `x=7 y=75 w=288 h=827`, and content body padding `68px 0 0
  288px`. After scroll, header and sidebar positions stayed pinned.
- Re-ran `pnpm --filter @doow/docs typecheck` and
  `pnpm --filter @doow/docs lint`.

## 2026-05-11T06:51Z | [doow-web] | [Codex]

- Corrected the docs desktop frame so the header and sidebar stay in the grid
  flow instead of being `fixed` overlays above the page.
- Added `apps/docs/src/styles/docs-layout-frame.css` as the final imported
  frame override for the desktop docs shell.
- Kept the browser/root document as the normal page scroller, made `body`
  overflow visible, and left the header/sidebar as `sticky` elements.
- Verified the sticky geometry in browser: after scroll, header top stayed at
  `7px` and sidebar top stayed at `6px`.
- Re-ran `pnpm --filter @doow/docs typecheck` and
  `pnpm --filter @doow/docs lint`.

## 2026-05-11T00:11Z | [doow-web] | [Codex]

- Repaired the desktop docs frame after the scroll experiment made the header,
  sidebar, and inner content feel disconnected.
- Kept the browser document as the normal page scroller while the docs header
  and sidebar remain fixed on desktop.
- Made the docs main pane and shell body explicitly own the shared frame
  background so the header, sidebar, and content read as one connected surface.
- Verified `@doow/docs` with `pnpm --filter @doow/docs typecheck`,
  `pnpm --filter @doow/docs lint`, and a fresh isolated browser screenshot.
