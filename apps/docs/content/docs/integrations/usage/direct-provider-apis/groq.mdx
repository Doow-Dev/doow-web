---
title: "Groq"
description: "Connect Groq usage and token metrics to Doow."
slug: "integrations/usage/direct-provider-apis/groq"
section: "integrations"
order: 313
status: "published"
updatedAt: "2026-05-15"
---
Connect Groq when Doow needs token usage and model activity from your Groq
account.

## What you need

- A Groq account with access to the **GroqCloud Console**.
- Access to Groq **Prometheus Metrics** for the organization.
- A Groq **API key** from the account that owns the usage.

<Callout title="Use an approved key">
  Groq documents Prometheus Metrics as an Enterprise feature. Before
  connecting, confirm that the account has metrics access and that the key is
  approved for Doow usage reporting. Doow does not need permission to change
  billing settings or manage unrelated account settings.
</Callout>

## Create an API key

1. Go to [console.groq.com](https://console.groq.com).
2. Open **API Keys**.
3. Create a new API key.
4. Name it something identifiable, such as `Doow usage read`.
5. Copy the key. Groq only shows the secret once.
6. Paste it into Doow when prompted.

## What Doow reads

| Metric | Description |
|---|---|
| `input_tokens_rate` | Input token consumption per model, per day |
| `output_tokens_rate` | Output token consumption per model, per day |
| Model | Which model was called |
| Project | Groq project ID (where available for team attribution) |

Usage is pulled from Groq's metrics API and aggregated at the day level.

## What Doow does not read

- The content of any prompts or completions
- Billing payment methods or invoices

## How to connect

1. Go to **Company Settings**, then **Integrations** in your Doow workspace.
2. Find **Groq** and select **Connect**.
3. Paste the API key.
4. Select **Connect**.
5. Wait for Doow to verify the key and begin the initial usage sync.

## Confirm the sync worked

After connecting, open the integration detail page from **Company Settings**, then **Integrations**. A healthy usage sync shows a connected state, a recent sync
timestamp, and usage rows for the expected account, project, model, resource,
or endpoint.

If the credential is accepted but no usage appears, verify that the key belongs
to the right Groq account and has access to the usage you expect Doow to read.

## Troubleshooting

Use these checks when usage data is missing or incomplete:

- Confirm the credential belongs to the provider organization you want Doow to read.
- Confirm the credential can read usage, billing, metrics, or organization-level data.
- Confirm the provider has usage in the selected time range.
- Check whether the provider reports usage with a delay.
- Check the integration event log for credential, scope, or rate-limit errors.

## Disconnecting

Go to **Company Settings**, then **Integrations**, find Groq, and select
**Disconnect**. Doow deletes the stored key immediately. Revoke the key in the
GroqCloud Console when you want to remove provider-side access too.

## Next steps

After usage data appears, connect an identity provider if Doow needs to match
Groq usage to users and app ownership. Connect accounting if the review needs
finance-confirmed spend.
