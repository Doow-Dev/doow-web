import React from "react";
import withPublicRedirectTo from "../../hoc/withPublicRedirectTo";
import { LoginView } from "../../views/login /login.view";

export default withPublicRedirectTo(LoginView);
